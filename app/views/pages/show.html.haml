#centre
  %h1= @page.title
  %ul#actions
    %li= link_to 'improve this page', edit_page_url(@page), :accesskey => "e"
    %li= link_to 'see who wrote which bits of this page', changes_page_url(@page)
    %li= follow_link

  = signed_off_message    
  = redirect_message
  = similar_pages_message
  
  #content
    - cache do
      = raw(@page.to_html)
  
  

#right= render 'right_bar'